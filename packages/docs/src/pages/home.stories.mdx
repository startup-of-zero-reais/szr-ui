import { Meta } from '@storybook/addon-docs'

<Meta title="Home" />

# UI

Design System da Startup de Zero reais

Para utilizar os componentes documentados você precisa instalar o pacote:

```sh
npm i @startup-of-zero-reais/react
```
ou então

```sh
yarn add @startup-of-zero-reais/react
```
